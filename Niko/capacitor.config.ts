import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.niko',
  appName: 'Niko',
  webDir: 'dist/niko',
  bundledWebRuntime: false
};

export default config;
